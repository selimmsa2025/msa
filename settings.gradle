pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        
        maven {
            url NEXUS_MAVEN_PUBLIC_URL
            setAllowInsecureProtocol(true)
            credentials { 
                username NEXUS_USERNAME
                password NEXUS_PASSWORD
            }
        }
        
    }
}
dependencyResolutionManagement {
    repositories {
        mavenCentral()
        /**maven { url "https://maven.egovframe.go.kr/maven/" } */
        maven { url "http://nexus-nexus3.iop-cicd.svc.cluster.local:8081" }

        mavenLocal()
        
        /**
        maven {
            url NEXUS_MAVEN_EGOV_URL
            setAllowInsecureProtocol(true)
            credentials {
                username NEXUS_USERNAME
                password NEXUS_PASSWORD
            }
            metadataSources {
                artifact()
            }
        }
        maven {
            url NEXUS_MAVEN_PUBLIC_URL
            setAllowInsecureProtocol(true)
            credentials {
                username NEXUS_USERNAME
                password NEXUS_PASSWORD
            }
        }
        */
    }
}

rootProject.name = 'iop-ci-catalog'
